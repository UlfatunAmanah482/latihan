<!DOCTYPE html>
<html>
  <%- include('partials/head') %>   
  <body>
    <%- include('partials/navbar') %> 
    <div class="container">
        <% users.forEach(user => { %>
            <div class="container">
                <h1>
                    <%= user.fullname %> 
                </h1>
                <p>
                    <%= user.email %> 
                </p>
            </div>
        <% }) %>

        <% useers.forEach(user => { %> <% if (user.fullname) { %>

        <% }}) %> 
    </div>
  </body>
</html>
